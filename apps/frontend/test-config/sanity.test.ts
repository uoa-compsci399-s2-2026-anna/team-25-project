import { describe, expect, it } from "vitest"

// Placeholder so `vitest run` has something to collect until real tests land.
describe("sanity", () => {
  it("runs", () => {
    expect(true).toBe(true)
  })
})
